{
  "name": "pulse-frontend",
  "version": "2.0.0",
  "description": "Pulse — frontend assets only. Backend is FastAPI.",
  "private": true,
  "scripts": {
    "note": "This project uses FastAPI backend. Run: uvicorn app.main:app"
  },
  "devDependencies": {}
}
